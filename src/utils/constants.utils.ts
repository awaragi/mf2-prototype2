/**
 * Application-wide constants
 */

/**
 * Custom header or URL parameter name to bypass content caching
 * When this flag is present in a request, the service worker will not cache the response
 */
export const NO_CACHE_FLAG = 'X-No-Cache';

